{
  "name": "open-api-db",
  "version": "1.0.0",
  "main": "src/index.ts",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@prisma/client": "6.8.2",
    "prisma": "6.8.2"
  },
  "scripts": {
    "build": "rm $DATABASE_PATH || yarn prisma generate && yarn prisma db push --force-reset && tsc"
  }
}
